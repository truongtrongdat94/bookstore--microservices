# Frontend Environment Configuration
# Copy this file to .env.local and fill in the values

# API URL Configuration
# ---------------------
# Development: Leave empty to use Vite proxy (/api -> localhost:3000)
# Production: Set to your API Gateway URL with HTTPS
#
# Examples:
#   Development: (leave empty or don't set)
#   Production: https://api.bookstore.com
#   Production: https://api.bookstore.com/api
VITE_API_URL=

# Note: All environment variables must be prefixed with VITE_ to be
# exposed to the client-side code in Vite applications.
