# API Gateway Environment Variables (Local Development)

# Server
NODE_ENV=development
PORT=3000

# JWT Secret (must match across all services)
JWT_SECRET=dev-jwt-secret-key

# Service URLs (for local development without Docker)
USER_SERVICE_URL=http://localhost:3001
BOOK_SERVICE_URL=http://localhost:3002
ORDER_SERVICE_URL=http://localhost:3003
NOTIFICATION_SERVICE_URL=http://localhost:3004

# Logging
LOG_LEVEL=info

# CORS Configuration
# Comma-separated list of allowed origins (e.g., https://bookstore.com,https://www.bookstore.com)
# Use * to allow all origins (development only)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
